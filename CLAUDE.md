# kaigo-data

厚労省の介護サービス情報公表システムのオープンデータから、東京都の居宅介護支援
事業所の架電リストを作るリポジトリ。3人（しょうちゃん・さくや・廣澤）が実際に
電話をかけ、結果を記録して評価する運用が乗っている。

## 進め方

### 作る前に仕様を出す

出力の形が変わる作業は、コードを書く前に仕様を文章で出して合意を取る。最低限
次を明示する。

- 何を入力にするか
- 出力するファイル名とシート名
- 列名と、その並び順
- プルダウンの選択肢
- 件数がどう変わるか（前回との差分）

「とりあえず作って後で直す」をやらない。**このリポジトリの成果物は人が業務で
使っているため、渡した後に名前や選択肢が変わると運用が壊れる。**

### 渡した後のものを黙って変えない

シート名・列名・プルダウンの選択肢・ファイル名は、いったん渡したら契約として
扱う。変更が必要なら、変更内容と影響範囲を先に伝えてから直す。

この契約は `scripts/output_spec.json` に固定してある。スクリプトは実行の最後に
実際の出力と照合し、食い違えばエラーで止まる。意図した変更のときだけ spec を
編集する。編集は git の差分に必ず現れるので、黙って変わることがない。

### 数字は必ず検証してから報告する

件数・率・電話番号を報告するときは、元データと照合した結果をセットで出す。
検証していない数字を断定で書かない。データに無い項目は「無い」と言い、
推測で埋めない。

推定を含む列（`事業所タイプ(推定)` など）は、列名と説明の両方に推定である
ことを書く。

## このデータで絶対に守ること

### 電話番号

架電リストなので、番号の誤りは実害に直結する。

- `電話番号` 列は元データの原文をそのまま入れる。改変しない
- 整形は `電話番号(整形)` に分けて出す
- 整形の前後で数字列が一致することを全件検証し、1件でも違えば実行を止める
- 電話番号・FAX・事業所番号・法人番号は文字列セルで書き出す（先頭ゼロ対策）
- 区切りのない番号は、桁の切り方が一意に決まる場合だけハイフンを補う。
  市外局番の桁数が一意でないもの（042系など）は触らない

### 絞り込み

絞り込みで落とした行は捨てず、理由をつけて `除外リスト` に残す。方針が変わった
ときに戻せるようにしておく。

## 元データについて

配布CSVは24列で、エンコーディングは **utf-8-sig**（cp932ではない）。全36,212行が
`サービスの種類 = 居宅介護支援`。

**元データに無い項目**: 代表者名 / 管理者名 / 指定年月日 / 法人所在地。
架電時の記入欄として空列で残している。取得元を探す場合は
`scripts/README.md` の候補を参照。

法人の名寄せは `法人番号` を使う（法人所在地が無いため）。東京都内は全件に
法人番号が入っている。

## 実行

```bash
pip install pandas openpyxl
python3 scripts/extract_tokyo_1houjin_1kyotaku.py <CSVパス>
```

詳細は `scripts/README.md`。

## ネットワーク

この環境は `www.mhlw.go.jp` への通信が遮断されている（egress ポリシー）。
CSVは手で用意してリポジトリに置くか、アップロードしてもらう。プロキシの
拒否は迂回しない。
